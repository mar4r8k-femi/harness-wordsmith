{
    "name": "Your Project",
    "image": "mcr.microsoft.com/vscode/devcontainers/base:ubuntu",  // Use your preferred base image
    "runArgs": ["--init"],
    "mounts": [
        "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
    ],
    "remoteUser": "vscode",
    "features": {
        "docker-in-docker": "latest"
    }
}
